import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
// the above imports React, ReactDOM, and the CSS file.

import App from './App';
// import App from './Api';

ReactDOM.render(<App />, document.getElementById('root'));
// App component is loaded here
